specVersion: "v1"
serviceId: "parkinscan"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "parkinscan-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
